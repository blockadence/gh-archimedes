# OS
.DS_Store
Thumbs.db

# Editors/IDEs
.idea/
.vscode/
*.swp
*.swo
*~

# Env/secrets
.env
.env.local
.env.*.local

# Misc
*.bak
*.log
/tmp/

# Run records, for anyone who runs a driver out of this checkout by hand
# rather than out of an instance. The suite itself points --root at a work
# directory of its own (tests/helpers.sh says why), so nothing here comes
# from running the tests.
/.archimedes-runs/

# Go CLI build output
/archimedes
/archimedes.exe

# Release artifacts, built into dist/ by .github/release-build.sh
/dist/
